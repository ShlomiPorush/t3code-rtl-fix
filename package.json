{
  "name": "t3code-rtl-fix",
  "version": "1.1.3",
  "private": true,
  "description": "Automatic per-message RTL styling for T3 Code on Windows",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js && node tests/cdp-pipe-smoke.js"
  },
  "engines": {
    "node": ">=18"
  }
}
